# Copy to .env and fill in. Both are optional — resolveWorkContext degrades
# a missing source to { ok: false, reason: "..." } instead of failing.

# GitHub personal access token with repo read scope (for PR / linked issue lookup).
GITHUB_TOKEN=

# Linear personal API key (for issueVcsBranchSearch lookup by branch name).
LINEAR_API_KEY=
